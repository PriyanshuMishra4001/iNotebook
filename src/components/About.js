import React from "react";
const About = () => {
  // const a = useContext(noteContext)
  // useEffect(()=>{
  //     a.update();
  //     // eslint-disable-next-line
  // },[])
  return (
    <div>
      {/* This is about {a.state.name} and {a.state.gender} is in class {a.state.class}. */}
      This is about page.
    </div>
  );
};

export default About;
